package com.phantom.player.ui.theme

import androidx.compose.ui.graphics.Color

// Dark theme colors - Extracted directly from the cyberpunk DJ image
val PhantomBlack = Color(0xFF0A0614)          // Deep purple-black from background
val PhantomDarkPurple = Color(0xFF1A0E2E)     // Dark purple from shadows
val PhantomMidPurple = Color(0xFF2D1B4E)      // Mid-tone purple from atmosphere

// Neon accent colors - Direct from the spectral DJ energy
val PhantomCyan = Color(0xFF00F0FF)           // Bright electric cyan (main energy)
val PhantomPurple = Color(0xFFB24BF3)         // Vivid purple (spectral body)
val PhantomPink = Color(0xFFFF1493)           // Hot pink/magenta (energy peaks)
val PhantomBlue = Color(0xFF0080FF)           // Electric blue (secondary glow)
val PhantomTeal = Color(0xFF00FFD1)           // Teal highlights (turntable glow)

// Neon sign colors from cityscape
val NeonViolet = Color(0xFF8B00FF)            // Violet neon signs
val NeonMagenta = Color(0xFFFF00FF)           // Magenta signs
val NeonLime = Color(0xFF39FF14)              // Lime green accents

// Surface colors - club/studio aesthetic
val SurfaceDark = Color(0xFF0D0A1A)           // Dark club floor
val SurfaceVariantDark = Color(0xFF1A1229)    // Equipment panels
val SurfaceGlass = Color(0x99160D2E)          // Glass/translucent panels

// Turntable/Equipment colors
val EquipmentMetal = Color(0xFF2A2438)        // Dark metal equipment
val EquipmentGlow = Color(0xFF6B4FBF)         // Purple equipment glow

// Light theme (minimal - app is dark-focused)
val PhantomLightGray = Color(0xFFF5F5F5)
val PhantomWhite = Color(0xFFFFFFFF)
